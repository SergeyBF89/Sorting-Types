using static Sortings;
using static Infastructure;
// int[] array = CreateArray(10);
// Show(array);
// SortSelection(array);
// Show(array);
10.CreateArray(min: 10, max: 30)
    .Show()
    .SortSelection()
    .Show();